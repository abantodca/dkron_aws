route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: default
    webhook_configs:
      - url: "http://host.docker.internal:9999/dummy"
        send_resolved: true